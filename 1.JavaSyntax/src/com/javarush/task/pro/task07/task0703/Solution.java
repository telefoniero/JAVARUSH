package com.javarush.task.pro.task07.task0703;

/* 
Минимальное целое
*/

public class Solution {
    byte byteNum = (byte) (-Math.pow(2, 7));
    short shortNum = (short) (-Math.pow(2, 15));
    int intNum = (int) (-Math.pow(2, 31));
    long longNum = (long) (-Math.pow(2, 63));
}
